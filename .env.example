# 本地开发：复制本文件为 `.env.local` 并填入自己的值（所有 .env* 都已被 .gitignore 忽略）。

# Mapbox 公共 token（pk.*，会打包进浏览器端）
# 获取地址：https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_public_token_here

# 部署时由 GitHub Actions 从仓库 Secret/Variable `NEXT_PUBLIC_MAPBOX_TOKEN` 注入，
# 设置位置：Settings → Secrets and variables → Actions。
